# Hook runner configuration
# ─────────────────────────────────────────────────────────

# Maximum number of Bash commands allowed per session before blocking
MAX_COMMANDS=50

# Warn the user when this threshold is reached (must be < MAX_COMMANDS)
WARNING_THRESHOLD=40

# Maximum number of timestamped backups to keep per file (oldest are deleted)
MAX_BACKUPS=5
